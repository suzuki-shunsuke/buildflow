---
# This file is imported from import_phases.yaml
- name: main 2
  tasks:
  - name: hello
    command:
      command: echo "main 2"
- name: main 3
  tasks:
  - name: hello
    command:
      command: echo "main 3"
